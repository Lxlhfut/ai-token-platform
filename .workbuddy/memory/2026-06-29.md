# 2026-06-29 工作日志

- 完成管理后台前端集成：新增「分组管理」「降级映射」「毛利报告」三个标签页及对应侧边栏入口。
- 在 app/static/app.js 中实现：
  - 分组 CRUD：loadGroups / showGroupForm / editGroupForm / setDefaultGroup / deleteGroup
  - 降级映射 CRUD：loadFallbacks / showFallbackForm / editFallbackForm / deleteFallback
  - 毛利报告：loadMarginReport（按分组展示成本/售价/利润）
  - 在 loadAdminPanel 中接入三组新数据加载
- 对接后端已有 /api/admin/groups、/api/admin/fallbacks、/api/admin/pricing/margin 端点。
- 已通过 lint 检查，无新增报错。因系统 CET 限制无法本地启动验证。
- 修复启动报错：model_pricing 表缺少 created_at 列。在 app/init_db.py 新增 `_add_column_if_missing` 通用迁移辅助函数，并统一为 model_pricing / model_groups / model_fallbacks / upstream_channels 补齐 created_at 列。
- 修复数据库路径问题：app/database.py 将相对 SQLite 路径解析为项目根目录绝对路径，避免 unable to open database file。
- 修复用户端模型定价「加载中」bug：
  - 原因：`app/static/app.js` 的 `loadUserPanel` 里使用 `pricing` 变量渲染，但从未调用 `api('/api/user/pricing')`。
  - 修复：在渲染定价表前补上 `const pricing = await api('/api/user/pricing');`。
- 将用户端模型定价从「概览」页独立到左侧导航栏「模型定价」页面：
  - `app/templates/dashboard.html`：侧边栏已有「模型定价」入口；在内容区新增 `pane-pricing` 标签页，并将定价卡片从 `pane-overview` 移入其中。
- 后台官方零售价字段确认已存在：`/admin` 的添加/编辑表单、列表展示均包含「官方零售价」。若浏览器未显示，请清缓存并重启服务。
- 修复 `/admin` 管理员登录点击无响应、账号密码被清空的问题：
  - 原因：`app/static/app.js` 中 `loadUserPanel` 同时通过 `Promise.all` 解构声明了 `pricing`，后面又用 `const pricing = await api('/api/user/pricing')` 重复声明，导致 JS 语法错误，整个 `app.js` 不执行，`initAdminDashboard` 未定义，表单以默认 GET 方式提交。
  - 修复：将 `Promise.all` 中的 `fetch('/api/user/pricing')` 改为 `api('/api/user/pricing')`，并删除后面重复的 `const pricing` 声明。
- 14:53 第二轮集成：全面实现上述所有功能的完整代码落地：
  - models.py: 新增 ModelGroup（分组）和 ModelFallback（降级映射）表，ModelPricing 添加 cost_price（上游成本）和 created_at 列
  - schemas.py: 新增 GroupCreate/Update/Out、FallbackCreate/Update/Out、MarginItem/MarginReport 等 Schema
  - init_db.py: 新增 `_add_column_if_missing` 通用迁移辅助函数
  - database.py: SQLite 相对路径 → 项目根目录绝对路径（避免 unable to open database file）
  - admin.py: 新增 /api/admin/groups（CRUD）、/api/admin/fallbacks（CRUD）、/api/admin/pricing/margin（毛利报告）共 9 个 API 端点
  - admin.html: 侧边栏新增「分组管理」「降级映射」「毛利报告」三个入口和对应标签页（含统计卡片+分组汇总+明细表格）
  - app.js: 实现 loadGroups/showGroupForm/editGroupForm/setDefaultGroup/deleteGroup、loadFallbacks/showFallbackForm/editFallbackForm/deleteFallback、loadMarginReport 全部函数，并在 loadAdminPanel 中自动加载；定价表单同步添加 cost_price 字段
  - dashboard.html: 用户端侧边栏新增「模型定价」入口，pane-pricing 标签页独立展示定价表
- 全部 6 个文件 lint 检查零错误。
- 修复启动 ImportError：创建缺失的 `app/routers/payment.py`，提供桩端点（微信支付返回 503「尚未配置」，支付宝返回 503「回调配置中」）。前端 JS 已有 try-catch 错误处理，用户会看到友好中文提示。微信支付 API 配置留空等待后续提供。
- `payment.py` 包含 4 个端点：POST /wechat/create、POST /alipay/create、GET /order/{order_no}、GET /qrcode
- 修复 payment.py 中 `import qrcode` 导致 ModuleNotFoundError：去掉 qrcode 依赖，注释掉 /qrcode 端点（后面 pip install qrcode 再启用）
- 模型定价增加「官方价 + 折扣」功能：
  - models.py: ModelPricing 新增 official_input_price / official_output_price 列
  - schemas.py: ModelPricingCreate/Update/Out 新增对应字段
  - init_db.py: 迁移补齐 official_input_price / official_output_price 列；默认定价数据含官方价（如 GPT-4o 官方 $2.5/$10，本站 8 折）
  - 管理后台：添加/编辑定价表单新增「官方输入价」「官方输出价」输入（与本站价并排展示）；定价列表展示官方价和折扣百分比
  - 用户端：dashboard 定价表新增「官方输入价」「官方输出价」「折扣」三列，绿色=有折扣、黄色=平价
- 删除「分组管理」功能（确认计费流程不使用分组倍率，直接按 ModelPricing 扣费）：
  - models.py: 删除 ModelGroup 模型
  - schemas.py: 删除 GroupCreate/GroupUpdate/GroupOut Schema；MarginItem/MarginReport 去掉 group_name 和 groups 字段
  - admin.py: 删除 5 个分组 CRUD 端点（GET/POST/PUT/DELETE/POST-default /api/admin/groups）；毛利报告去掉分组聚合逻辑
  - init_db.py: 移除 model_groups 迁移行
  - admin.html: 移除侧边栏「分组管理」按钮 + pane-groups 标签页 + 毛利报告「分组汇总」区块 + 表格「分组」列
  - app.js: 删除 loadGroups/showGroupForm/editGroupForm/setDefaultGroup/deleteGroup 及 loadGroups() 调用；loadMarginReport 简化去掉分组渲染
  - 计费链路不受影响：proxy_service.py 仍是 get_model_pricing → calculate_cost → deduct_balance，纯按模型定价扣款
- 微信支付 Native 扫码支付接入完成（2026-06-29）：
  - 凭证已配置于 .env：商户号 1747479956、AppID wx3dba70da859f70c2（小程序）、回调 https://ylxunlapi.top/api/payment/wechat/notify
  - app/services/wechatpay.py：手动实现微信支付 V3 API（RSA-SHA256 签名 + AES-256-GCM 解密回调），不依赖第三方 SDK
  - app/routers/payment.py：POST /api/payment/wechat/create（下单）、POST /api/payment/wechat/notify（回调）、GET /api/payment/qrcode（生成支付二维码PNG）
  - app/config.py：新增 wechat_private_key_pem 属性，自动将 .env 中 \n 转为真实换行
  - 依赖：qrcode[pil]、cryptography 已安装并加入 requirements.txt
  - 前端：dashboard.html 已含微信支付面板（金额选择 + 弹窗二维码 + 2秒轮询 + 自动刷新余额），app.js 已含 createWechatPayment/startPollWechatOrder/closePaymentModal
  - 支付流程：用户选金额 → 后端调用 /v3/pay/transactions/native → 前端展示二维码 → 用户扫码支付 → 微信回调 notify → 验签解密 → recharge_balance 自动到账
  - 部署注意事项：服务器必须有 HTTPS + 443端口；Nginx/Caddy 需正确转发 /api/payment/wechat/notify 路径
- 修复登录后无响应 bug（ResponseValidationError: official_input_price / official_output_price 要求 float 但收到 None）：
  - 根因：model_pricing 表中 7 条旧数据的 official_input_price 和 official_output_price 为 NULL（_add_column_if_missing 迁移只加列未回填默认值），前端登录后调用 /api/user/pricing 时 Schema 校验失败 → 整个 app.js 静默崩溃 → 登录按钮无响应
  - 数据库修复：UPDATE model_pricing SET official_input_price=0.0, official_output_price=0.0 WHERE ... IS NULL（14 行）
  - Schema 防御：ModelPricingOut 新增 @field_validator('official_input_price','official_output_price','cost_price', mode='before') _none_to_zero，将来再有 NULL 也不会崩溃
- 微信支付 & 支付宝支持任意金额充值（2026-06-29 16:35）：
  - dashboard.html：两个支付面板各新增 <input type=number min=1 step=0.01> 自定义金额输入框 + ¥5/10/20/50/100/500 快捷预设按钮
  - app.js：新增 getWechatAmount()/getAlipayAmount() 从输入框读取金额；handleWechatAmountInput()/handleAlipayAmountInput() 实时校验启用/禁用按钮；selectWechatAmount/selectAlipayAmount 改为写入输入框并高亮预设按钮；createWechatPayment/createAlipayPayment 改用 get*Amount() 取金额
  - 后端无需改动（/api/payment/wechat/create 和 /alipay/create 只要求 amount > 0）
- 支付宝电脑网站支付已集成（2026-06-29 16:48）：
  - 凭证来源：(2).env 备份文件，已合并到 .env：AppID=2021006167678173 + 应用私钥 + 支付宝公钥 + 回调/同步跳转 URL
  - 代码均早已完备：app/services/alipay.py（RSA2-SHA256 签名 + 异步通知验签）、app/routers/payment.py（POST /alipay/create + POST /alipay/notify + GET /alipay/return + GET /alipay/status）
  - 前端：dashboard.html 已有支付宝支付面板（含自定义金额输入 + 快捷预设），app.js 已有 createAlipayPayment() 跳转支付
  - 支付流程：用户输金额 → POST /api/payment/alipay/create → 返回 pay_url → 前端 window.location.href 跳转支付宝收银台 → 支付完成 → 支付宝 POST 回调 notify → 验签 → recharge_balance 自动到账
  - 双支付均已配置且运行正常（WeChat configured: True, Alipay configured: True）














